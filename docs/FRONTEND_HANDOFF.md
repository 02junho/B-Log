# 공개 포트폴리오 UI 인수인계

기준: 2026-09-12, main `e5bcbc2`에서 생성한 `codex/public-portfolio-ui`.
담당: 역할 B @Aio1135의 프론트엔드 지원. P3 전체 담당을 변경한 것은 아니다.

## 팀 현황

| 영역 | 확인된 상태 | 다음 작업 |
| --- | --- | --- |
| P1 파서·파이프라인 | 공통 스키마·태깅·빌더 완료. 랩탑 작업 PR #12도 main 반영 | 통합 오류 대응 |
| 역할 B | Codex 실로그 검증 및 병렬 실패 처리 main 반영. 이번 브랜치에서 공개 UI 구현 | PR 리뷰 대응, 발행 데이터 화면 E2E 지원 |
| P2 데이터·백엔드 | 업로드·잡·매칭·발행 API 구현. ROADMAP에 9/10 프로덕션 E2E 완료 기록 | 검수 GET API, 검수 확정 후 발행, GitHub OAuth |
| P3 프론트 | 기준 main에는 제품 화면 미완료. 이번 브랜치에 공개 페이지·랜딩·모바일 구현 | 기존 로컬 작업과 통합, 업로드·진행·검수·로그인 화면, OG 이미지 |
| P4 품질·데모 | 정규식 마스킹 시작점 및 평가 도구 있음 | 규칙 보강·테스트, 프롬프트 전후 평가, LLM 마스킹, 데모 3종 |

위 상태는 저장소 코드와 기록 기준이다. 팀원 개인의 미공개 작업이나 현재 프로덕션 상태까지 재검증한 것은 아니다.

## 구현 범위

- `/`: 서비스 소개와 동작하는 데모 진입 링크.
- `/p/sample-login-fix`: DB 없이 기존 합성 fixture로 표시. 합성 데이터임을 명시하고 예시 커밋 링크는 비활성화.
- `/p/[slug]`: 서버에서 `published_at`이 있는 `portfolios.view`만 조회. 원본 세션 로그는 조회하지 않음.
- 4단계 요약, 하이라이트와 선정 근거 펼치기, 단계 필터, 타임라인 인용·커밋, 통계·도구·분석 등급.
- 링크 복사 및 클립보드 거부 시 수동 복사, 로딩·오류·없는 페이지·빈 기록 처리, 모바일 화면.
- 기존 `PortfolioView` 계약 유지. 저장 데이터 표시 전 검증, 한글 slug 지원, GitHub 커밋 URL 제한.
- 이벤트 수를 기여도 점수로 표시하지 않으며 기록이 0개이면 비율 막대를 채우지 않음.

## 연결할 작업

1. P2: `GET /api/sessions/[id]/review`와 소유자 인증을 구현하고 응답 계약을 합의한다. 마스킹된 미리보기, 검수 상태, 확정 요청 및 실패 처리 방식이 필요하다.
2. P2·P3: 현재 즉시 발행 경로를 검수 확정 후 발행 흐름으로 연결한다. 프론트엔드의 버튼만으로 검수를 강제하지 말고 서버에서도 확인한다.
3. P2: GitHub OAuth를 연결한다. `BLOG_API_TOKEN`은 서버·CLI 전용이며 클라이언트 코드에 전달하지 않는다.
4. 역할 B·P3: 이 브랜치 통합 후 실제 발행 slug로 화면 E2E 및 Preview 배포 확인. 업로드·진행·검수 화면을 이어서 구현한다.
5. P4: 데모로 공개할 데이터를 검수하고 마스킹 품질 및 인용 프롬프트를 개선한다.

Step 7 전체 완료나 배포 완료로 표시하지 않는다. OG 이미지와 실제 데이터 E2E도 남아 있다.

## 검증

- `npm test`: 74개 통과, 선택 실로그 2개 생략.
- `npm run lint`, `npm run typecheck` 통과. 프로덕션 빌드 성공 확인.
- 로컬 Chromium: 데스크톱 1440px·모바일 390px, 가로 넘침 없음, 단계 필터·선정 근거·링크 복사·랜딩 이동·없는 페이지 확인, 브라우저 오류 없음.
- 실제 DB 발행 결과의 화면 E2E 및 Vercel 배포는 이번 검증 범위에 포함하지 않음.
